#!/usr/bin/env bash
set -euo pipefail

# Git pre-commit hook: verify no plaintext secrets are being committed
REPO_ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
"$REPO_ROOT_DIR/scripts/verify-no-plaintext-secrets.sh"
