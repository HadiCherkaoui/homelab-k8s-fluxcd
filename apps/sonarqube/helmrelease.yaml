# SPDX-FileCopyrightText: Hadi Cherkaoui <contact@hide.cherkaoui.ch>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarqube
  namespace: sonarqube
spec:
  interval: 10m
  chart:
    spec:
      chart: sonarqube
      version: 2026.4.0
      sourceRef:
        kind: HelmRepository
        name: sonarqube
        namespace: flux-system
  install:
    createNamespace: false
  valuesFrom:
    - kind: Secret
      name: sonarqube-postgres-cluster-app
      valuesKey: jdbc-uri
      targetPath: jdbcOverwrite.jdbcUrl
      optional: false
    - kind: Secret
      name: sonarqube-postgres-cluster-app
      valuesKey: username
      targetPath: jdbcOverwrite.jdbcUsername
      optional: false
  values:
    community:
      enabled: true
    ingress:
      enabled: false
    monitoringPasscode: "REDACTED-ROTATED"
    sonarProperties:
      sonar.core.serverBaseURL: "https://sonarqube.cherkaoui.ch"
    postgresql:
      enabled: false
    jdbcOverwrite:
      enabled: true
