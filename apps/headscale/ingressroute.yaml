apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: headscale
  namespace: headscale
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`headscale.cherkaoui.ch`)
      services:
        - name: headscale
          port: 8080
  tls:
    certResolver: letsencrypt
