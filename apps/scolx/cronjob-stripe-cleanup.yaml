apiVersion: batch/v1
kind: CronJob
metadata:
  name: scolx-stripe-cleanup
  namespace: scolx
spec:
  schedule: "0 0 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: scolx-stripe-cleanup
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: scolx-registry
          containers:
            - name: stripe-cleanup
              image: registry.cherkaoui.ch/scolx/scolx-v2/jobs:latest
              args: ["cleanup-stripe-events"]
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: scolx-postgres-cluster-app
                      key: fqdn-uri
              envFrom:
                - configMapRef:
                    name: scolx-server-config
                - secretRef:
                    name: scolx-backend-env
