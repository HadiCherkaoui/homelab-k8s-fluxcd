apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: defguard-proxy
  namespace: defguard
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`enroller-grpc.defguard.cherkaoui.ch`)
      kind: Rule
      middlewares:
        - name: security-headers
          namespace: traefik
      services:
        - name: defguard-defguard-proxy-grpc
          port: grpc
          scheme: h2c
  tls:
    certResolver: letsencrypt
