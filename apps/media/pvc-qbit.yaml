apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbit-media
  namespace: media
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: fast
  resources:
    requests:
      storage: 1Ti
