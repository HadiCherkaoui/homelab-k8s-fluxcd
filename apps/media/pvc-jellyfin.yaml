apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-media
  namespace: media
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: tank
  resources:
    requests:
      storage: 8Ti
