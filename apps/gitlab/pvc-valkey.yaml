apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valkey-data
  namespace: gitlab
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: fast
  resources:
    requests:
      storage: 4Gi
