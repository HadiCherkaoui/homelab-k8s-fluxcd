apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs-data
  namespace: gitlab
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: tank
  resources:
    requests:
      storage: 60Gi
